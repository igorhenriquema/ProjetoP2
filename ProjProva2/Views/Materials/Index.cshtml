@model IEnumerable<ProjProva2.Material>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UnidadeMedida.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UnidadeMedida.descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idMaterial }) |
            @Html.ActionLink("Details", "Details", new { id=item.idMaterial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idMaterial })
        </td>
    </tr>
}

</table>
